name: Latest blog post workflow
on:
  schedule: # Run workflow automatically
    - cron: '0 * * * *' # Runs every hour, on the hour
  workflow_dispatch: # Run workflow manually (without waiting for the cron to be called), through the Github Actions Workflow page directly
jobs:
  update-readme-with-blog:
    name: Magic README Update  with latest blog posts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: rodcarvalho/blog-post-workflow@master
        with:
          comment_tag_name: "Tutorial"
          commit_message: "Updated readme with the latest blog posts"
          enable_keepalive: true
          max_post_count: 5
          feed_list: "https://www.rodcarvalho.dev/posts/"
